server:
  port: 9083

spring:
  datasource:
    url: jdbc:mysql://localhost:3309/db_mascotas?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: vet_user
    password: vet_pass
    driver-class-name: com.mysql.cj.jdbc.Driver

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9761/eureka/
  instance:
    prefer-ip-address: true